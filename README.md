#build images

This project is about building and running images

And we will run it in Kubernetes on a later stage, but first locally on our machine.

In order to run the Dockerfile, use:

sudo docker build -t my-python-app .